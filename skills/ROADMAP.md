# hscredit Skills 规划

以下目录当前只含 `.gitkeep`，没有 `SKILL.md`，Agent 不应发现或调用：

| 目录 | 规划能力 | 状态 |
|---|---|---|
| `hscredit` | 总入口和跨领域路由 | 规划中 |
| `hsmodel` | 模型、AutoTuner、Loss、ScoreCard 和规则模型 | 规划中 |
| `hsrule` | Rule、RuleFlow、表达式和规则分析 | 规划中 |
| `hsselect` | 所有公开 Selector 和筛选报告 | 规划中 |
| `hsviz` | 全部可视化能力，包括分箱图 | 规划中 |
| `hsexcel` | ExcelWriter、dataframe2excel 和多 Sheet 组装 | 规划中 |

后续还可拆分 EDA、模型监控、编码器、特征工程、拒绝推断、金融计算和模型上线导出 Skill。只有完成代码、Schema、说明和真实行为测试后，规划项才能进入 `CATALOG.md`。
