# HSCredit 代码审查报告

> 审查日期：2026-06-20（本次复核，更新自同日早期版本）
> 审查范围：① 当前未提交改动（`hscredit/report/model_report.py` 入模变量超链接功能等 4 个文件）② 针对性专项复核：`core/viz` 可视化模块的 `ax`/`fig` 处理模式、`pyproject.toml` 依赖声明与安装行为、各模块文档示例（`Example:`/`**参考样例**`）可执行性 ③ 对上一版报告遗留问题（C-1~C-6）逐项重新验证
> 方法：人工逐行核对 + 多 agent 独立交叉验证（每项结论至少二次确认）+ 运行时复现（`/Users/xiaoxi/anaconda3/bin/python3`，`hscredit` 可编辑安装）+ `pip install --dry-run` 实际安装行为验证 + 全量 `pytest` 回归
> 对比基线：本文件同日早期版本（见下方"三、对历史遗留问题的重新验证"）

---

## 执行摘要

本次复核发现 **1 项严重问题（全新安装环境下 `import hscredit` 直接失败）**、**3 项高优先级功能缺陷**（可视化模块多个函数在按文档传参时崩溃或静默失效）、**1 项安装文档与实际打包配置不一致**的问题，以及若干文档示例与真实行为不符的情况（第一节）。当前未提交的改动（入模变量重要性表与有效性分析章节互相跳转的超链接功能）经验证**设计正确、19 个相关回归测试全部通过**，但存在 1 处与同函数其他代码不一致的异常处理疏漏（第二节）。

此外，本次对上一版报告列出的 C-1~C-6 逐项重新验证后发现：**C-1、C-2、C-3、C-5、C-6 在当前仓库源码中均已不存在**（与上一版"仍待处理"的结论不符，详见第三节的逐条证据），只有 C-4（`print` 用量）部分改善。这说明上一版报告的"实测"结论与当前本地仓库代码存在偏差，建议今后统一固定使用 `pip install -e .` 可编辑安装本仓库后再测试，避免基线漂移。

> **修复状态更新**：本节 N-1、N-2、N-3、N-5、N-6（三处文档示例）以及第二节 Diff-2、Diff-3 **均已在本次会话中修复并通过验证**（逐条核对实际报错复现 → 修复 → 重新运行复现脚本/单测确认；全量回归 `pytest tests/ -m "not slow and not integration"` 结果为 463 passed, 3 skipped, 2 failed——失败项与改动无关，见第四节）。各条目下方新增"**修复**"小节说明具体改动。N-4（CLAUDE.md 安装命令）在本节修复前已由外部改为 `pip install hscredit[boost]`，确认与 `pyproject.toml` 一致、无残留，故不再单列小节。N-7（Python 3.14 兼容性声明）按报告原建议属于"需要 CI 矩阵验证"而非可直接修改的代码缺陷，本次未改动。C-4（412 处 `print`）经确认后按原计划本轮跳过，留作后续持续清理项。

---

## 一、本次新发现问题

### N-1（P0，严重，✅已修复）干净环境下 `import hscredit` 必定失败：硬编码依赖未声明的 `six`

- **证据**：[`hscredit/core/viz/binning_plots.py:14`](../hscredit/core/viz/binning_plots.py#L14) 模块顶层无条件 `import six`，且 `six` **不在** `pyproject.toml` 的 `dependencies` 或任何 `optional-dependencies` 分组中。`hscredit/__init__.py:43` → `core/viz/__init__.py:41` → `binning_plots.py:14` 是 `import hscredit` 的必经路径（非按需懒加载）。
- **实测**：逐一确认 `numpy`/`pandas`/`scipy`/`scikit-learn`/`statsmodels`/`joblib`/`openpyxl`/`matplotlib`/`seaborn`/`tqdm`/`IPython`/`Cython`（全部声明的基础依赖）均不传递依赖 `six`（`pip show` 核实 `Requires` 字段均不含 six）。即：仅安装 `pip install hscredit` 声明的基础依赖、且环境中没有其他原因带入 `six` 时，`import hscredit` 会直接 `ModuleNotFoundError: No module named 'six'`。本机能"正常"运行是因为 anaconda base 环境里另有他用而预装了 six，掩盖了这个问题。
- **建议**：将 `import six` 替换为标准库等价写法（`six.iteritems(d)` → `d.items()`，本仓库目标 Python 3.9+，无需兼容 Python 2），或将 `six` 正式加入 `pyproject.toml` 的 `dependencies`。
- **修复**：[`binning_plots.py:14`](../hscredit/core/viz/binning_plots.py#L14) 移除 `import six`；唯一用法 [`binning_plots.py:1155`](../hscredit/core/viz/binning_plots.py#L1155) 由 `six.iteritems(mpl_table._cells)` 改为 `mpl_table._cells.items()`。已确认全仓库无其他 `six` 引用。

### N-2（P1，高，功能缺陷，✅已修复）`dataframe_plot`/`score_bin_plot` 在文档化的默认调用方式下崩溃

- **证据**：[`hscredit/core/viz/binning_plots.py:1108-1169`](../hscredit/core/viz/binning_plots.py#L1108) `dataframe_plot()` 仅在 `if ax is None:`（第1142行）分支内赋值 `fig`，但第1163/1167/1169行（`fig.tight_layout()`/`fig.savefig()`/`return fig`）在 `ax` 由调用方传入时无条件引用 `fig`，缺少 `else: fig = ax.get_figure()` 兜底。
- **实测**：[`hscredit/core/viz/risk_plots.py:725-732`](../hscredit/core/viz/risk_plots.py#L725) 中 `score_bin_plot()`（`hscredit.core.viz` 公开导出的文档化函数）以默认参数 `show_table=True` 调用 `dataframe_plot(..., ax=ax_table)`，必然触发 `UnboundLocalError: local variable 'fig' referenced before assignment`。已用 `examples/hscredit_yyp.xlsx` 真实数据复现。**当前未提交的 `examples/07_viz.ipynb` 改动中，调用 `score_bin_plot` 的演示单元格被整段注释掉**——这正是该缺陷在真实笔记本运行中触发后，被绕开而非修复根因的直接证据。
- **建议**：在 `dataframe_plot` 的 `if ax is None:` 分支补充 `else: fig = ax.get_figure()`。
- **修复**：[`binning_plots.py:1141-1145`](../hscredit/core/viz/binning_plots.py#L1141) 按建议补上 `else: fig = ax.get_figure()`。已用真实数据复现原先的 `UnboundLocalError`，确认修复后 `score_bin_plot(df_model, score_col='prob', target_col='target', n_bins=10)` 正常返回 `Figure`。

### N-3（P1，高，功能缺陷，✅已修复）`vintage_plot`/`bad_rate_trend_plot` 静默忽略调用方传入的 `ax`

- **证据 1**：[`hscredit/core/viz/risk_plots.py:982-984`](../hscredit/core/viz/risk_plots.py#L982) `vintage_plot()` 当 `show_heatmap=True` 时无条件 `fig, (ax_line, ax_heat) = plt.subplots(...)`，完全无视调用方传入的 `ax` 参数。
- **证据 2（更严重，因为是默认参数即触发）**：[`hscredit/core/viz/risk_plots.py:1246-1250`](../hscredit/core/viz/risk_plots.py#L1246) `bad_rate_trend_plot()` 的 `show_sample_count` **默认就是 `True`**（函数签名第1222行），该分支同样无条件创建新 `fig`/`ax`，调用方传入的 `ax=` 被完全丢弃。
- **实测**：分别构造已有 `ax` 调用两个函数，确认返回的 `Figure` 对象与传入的 `ax` 所属 `Figure` 不同，且传入的 `ax` 上未绘制任何内容；对照组 `show_heatmap=False`/`show_sample_count=False` 时行为正确（确认 bug 仅限这两个分支，而非全函数）。
- **影响**：这两个函数的签名都声明支持 `ax` 参数用于"画到我已有的子图网格里"，但在常见配置下该承诺没有兑现，属于典型的"功能实现与文档预期不符"。
- **建议**：两处都应改为先决定 `fig`/主轴（复用传入的 `ax` 或其 `ax.get_figure()`），再决定是否额外创建副轴用于热力图/样本数柱状图，而不是整体重新 `plt.subplots()`。
- **修复**：两处都改为「传入 `ax` 时复用该 `ax` 作主轴，副轴改用 `mpl_toolkits.axes_grid1.make_axes_locatable(ax_line).append_axes(...)` 挂载在同一 `figure` 上」，而不是整体重新 `plt.subplots()`：[`vintage_plot`（risk_plots.py:981起）](../hscredit/core/viz/risk_plots.py#L981) 用 `append_axes("right", size="40%", pad=0.8)` 接热力图；[`bad_rate_trend_plot`（risk_plots.py:1253起）](../hscredit/core/viz/risk_plots.py#L1253) 用 `append_axes("bottom", size="33%", pad=0.1, sharex=ax_line)` 接样本数柱状图。`ax=None` 时原有行为不变。已用合成数据验证：传入 `ax` 后返回的 `Figure` 与该 `ax` 所属 `Figure` 一致，且主轴上确实绘制了内容。

> **N-4 说明**：CLAUDE.md 的安装命令已在本次修复前由外部改为 `pip install hscredit[boost]`，与 `pyproject.toml` 一致，已确认无残留，不再单独列出对应章节。

### N-5（P2，依赖卫生，✅已修复）`Cython` 被列为运行时硬依赖，但代码中未发现任何使用

- **证据**：[`pyproject.toml:57`](../pyproject.toml#L57) 中 `"Cython>=0.29.0"` 位于 `[project.dependencies]`（基础运行依赖，非 build 依赖）。全仓库搜索确认：无 `.pyx`/`.pxd` 文件，`hscredit/` 内无 `import Cython`/`from Cython`，`setup.py` 是纯 `setup()` 调用，未使用 `cythonize`/`build_ext`。
- **影响**：每个用户安装 `hscredit` 都被迫多装一个完全用不到的包，增加安装体积和潜在的版本冲突面。
- **建议**：未来计划引入 Cython 扩展，应放在 `build-system.requires` 而非运行时依赖
- **修复**：[`pyproject.toml`](../pyproject.toml#L45) `[project.dependencies]` 中移除 `"Cython>=0.29.0"` 一行。

### N-6（P2，文档示例与实现不符，✅已修复）

- **`hscredit/report/overdue_predictor.py:84` 与 `:881`**：docstring 示例 `from hscredit.report.overdue_estimator import OverduePredictor`（及 `overdue_estimation_report`）引用了不存在的模块 `overdue_estimator`，实际模块名是 `overdue_predictor`。两处都是同样的复制粘贴笔误，按文档原样执行会 `ModuleNotFoundError`。**修复**：两处导入路径改为 `from hscredit.report.overdue_predictor import ...`；第881行额外发现函数名也写错（`overdue_estimation_report` 实际不存在，正确名称是 `overdue_prediction_report`，与示例下方实际调用一致），一并修正。已验证两处 `>>> from ... import ...` 均可正常执行。
- **`hscredit/core/eda/correlation.py:58` vs `:82`**：`high_correlation_pairs()` 的示例固定写 `print(pairs[['特征1', '特征2', '相关系数', '相关评级']])`（第58行），但当没有特征对达到 `threshold`（随机数据下很常见，也是默认行为）时，函数实际返回 `pd.DataFrame({'信息': [...]})`（第82行，单列，无上述四列），照抄示例会 `KeyError`。docstring 未提及这个双返回结构的分支。**修复**：`:return:` 说明补充两种返回结构；Example 改为 `if '特征1' in pairs.columns: ... else: print(pairs['信息'].iloc[0])` 的安全写法，已用合成数据验证两条分支都能正确执行。
- **`hscredit/report/swap_analysis.py:1012-1019`**：顶层便捷函数 `swap_analysis()` 的 `**参考样例**` 直接对裸 `swap_df`/`reference_df` 调用，但函数内部（`SwapAnalyzer.analyze()`，约第324行）要求 `swap_df` 已包含 `swap_type_col`（默认列名 `"swap_type"`）这一派生列，该列的构造逻辑在另一个 helper（约第929行 `np.select`）中，`swap_analysis()` 本身不会自动调用它。按示例原样跑会在 `analyze()` 内部 `KeyError: 'swap_type'`。这一点函数的 `:param swap_df:` 说明里其实提了一句（"含swap_type和score"），但 Example 代码块没有展示如何构造，属于示例不完整而非逻辑错误，严重度低于前两条。**修复**：Example 开头补充 `swap_df = create_swap_dataset(raw_df, original_rule_col=..., new_rule_col=..., score_col='score')` 步骤，并说明也可用 `create_swap_dataset_from_rules`。已用合成数据跑通从 `create_swap_dataset` 到 `swap_analysis(...).summary_report_count` 的完整链路。

### N-7（P3，提示，证据较弱）`requires-python` 声称的 Python 版本上限与依赖下限存在潜在张力

`pyproject.toml` 声明 `requires-python = ">=3.9,<3.15"`（即支持到 Python 3.14），但基础依赖下限 `numpy>=1.23.0` 对应的版本系列并不提供 Python 3.14 的预编译 wheel（NumPy 1.23.x 仅支持到 3.11）。pip 在 3.14 上会自动解析到更新的 numpy 2.x，实际不会装到 1.23.0，所以不构成立即崩溃，只是版本声明本身存在"声明下限其实装不上目标 Python 版本"的不自洽，建议在 CI 矩阵里对 Python 3.14 + 最低版本依赖组合做一次实际验证。

---

## 二、当前未提交改动（diff）复核

变更内容：`hscredit/report/model_report.py`（Excel 报告 "3-入模变量分析" Sheet）新增"重要性汇总表 ↔ 各特征有效性分析小节"互相跳转的超链接；`tests/test_report/test_model_report.py` 同步更新断言；`examples/11_report.ipynb` 为建模数据补充 `放款时间` 列；`examples/07_viz.ipynb` 主要是图片输出 diff 噪声，唯一真实代码改动是注释掉 `score_bin_plot` 调用（即上文 N-2 的现场证据）。

### Diff-1（设计验证：通过）行/列定位算法正确

手工逐行核对 `feature_name_col = 2 + features_summary.columns.get_loc("特征名")` 与 `features_summary_rows = {... : features_summary_start_row + features_summary.columns.nlevels + position ...}` 两个公式，对照 `hscredit/excel/writer.py` 中 `dataframe2excel`/`insert_df2sheet` 的真实落列/落行规则（`start_col + col_idx`，无 `index=True` 时不偏移；表头恰好占 `columns.nlevels` 行）逐一验证，对"主路径"（`_get_features_summary()` 走 `df.summary()`，"特征名"本就是第0列）和"降级路径"（走 `get_features_describe()`，需要 `reset_index().rename(...)` 补回"特征名"列）都成立。`tests/test_report/` 全部 19 个测试通过。

### Diff-2（P2，一致性疏漏，✅已修复）新增的两处超链接写入缺少与同函数其余代码一致的异常防护

- **证据**：同一方法内，几乎所有 `writer.*` 调用都包了 `try/except Exception: pass`（例如紧邻的 [model_report.py:1999-2001](../hscredit/report/model_report.py#L1999) 对**同一个** `insert_hyperlink2sheet` API 的调用，以及 2076/2092/2117/2152/2177/2185/2195/2205 行），唯独本次新增的两处（[model_report.py:2053-2057 与 2058-2062](../hscredit/report/model_report.py#L2053)）是裸调用。
- **影响**：这段代码的既有设计意图是"单个图表/表格写入失败不应中断整份报告生成"。一旦 `insert_hyperlink2sheet` 在某些边界输入下抛异常（例如目标单元格超出已写入范围），会导致整份 Excel 报告生成中断，而不是像周围代码一样静默跳过这一个超链接，行为与同一函数里其余 9 处写入调用不一致。
- **建议**：按同函数既有写法包一层 `try/except Exception: pass`。
- **修复**：[`model_report.py:2052-2064`](../hscredit/report/model_report.py#L2052) 给两处 `insert_hyperlink2sheet` 调用包上 `try/except Exception: pass`，与同函数其余写入调用风格一致。`tests/test_report/` 全部 20 个测试（含新增的 Diff-3 测试）通过。

### Diff-3（P3，测试覆盖缺口，✅已修复）`summary_row is None` 分支无断言覆盖

新增测试仅使用单特征 `f0`（必定同时存在于 `feature_list` 与 `features_summary["特征名"]` 中），未覆盖 `features_summary_rows.get(str(feat))` 返回 `None`（例如某特征被 `_get_features_summary()` 内部静默丢弃，但仍留在 `feature_list` 里）的情况。代码逻辑上有 `if summary_row is not None:` 防护、不会崩溃，但这条路径目前完全没有测试断言覆盖，回归该 guard 不会被发现。

- **修复**：新增 [`test_excel_skips_hyperlink_when_feature_missing_from_summary`](../tests/test_report/test_model_report.py#L395)，通过 `monkeypatch` 让 `_get_features_summary()` 返回缺少 `f0` 的空表，断言 `to_excel` 不抛异常且对应小节标题单元格的 `hyperlink` 为 `None`。已运行通过。

---

## 三、对历史遗留问题的重新验证

以下是本文件**同日早期版本**第三节列出的 C-1~C-6，逐项对当前仓库源码重新验证的结果：

| 编号 | 早期版本结论 | 本次复核结果 | 复核证据 |
|------|------|------|------|
| C-1 | `FeatureImportanceSelector` 不支持仅有 `coef_` 的模型，`test_with_sklearn_lr` 失败 | ✅ **已不存在** | [`importance_selector.py:67`](../hscredit/core/selectors/importance_selector.py#L67) 与 [`rfe_selector.py:111`](../hscredit/core/selectors/rfe_selector.py#L111) 当前均为 `importance_getter='auto'`；`test_selector_model_compat.py` 中全部 5 个 `test_with_sklearn_lr` 变体（含 `TestFeatureImportanceSelectorCompat`）**全部 PASSED** |
| C-2 | `SingleFeatureRuleMiner` docstring/CLAUDE.md 示范不存在的 `method='optimal_iv'` | ✅ **已不存在** | [`single_feature.py:62`](../hscredit/report/mining/single_feature.py#L62) 当前示例与 [`CLAUDE.md:151`](../CLAUDE.md#L151) 均使用合法值 `'best_iv'`，未发现 `optimal_iv`/`cp_sat` 残留 |
| C-3 | 裸 `except:` 34 处 | ✅ **已不存在** | 全仓库严格正则 `^\s*except\s*:` 命中 **0** 处 |
| C-4 | `print(` 517 处 | ⚠️ **部分改善** | 当前 **412** 处，仍建议迁移至 `logging`（项目已有 `utils.logger`） |
| C-5 | `core/binning/base.py:1858` `plot()` 为 TODO 占位 | ✅ **已不存在** | [`base.py:1838-1867`](../hscredit/core/binning/base.py#L1838) 当前已实现，委托给 `core.viz.bin_plot` |
| C-6 | flake8 报告若干未用局部变量（`best_ks`/`kmeans`/`mdlp`/`or_binning`/`monotonic`） | ✅ **已不存在** | 对应文件单独运行 `flake8 --select=F841` 命中 **0** 处（同一批文件的 F401 未用导入仍有残留，但那不在 C-6 范围内） |

**说明**：C-1/C-2/C-3/C-5/C-6 在当前本地仓库源码中均已找不到对应证据，与早期版本"仍待处理"的结论相反。由于本次复核统一使用 `pip install -e .` 方式对**本仓库当前源码**做可编辑安装后实测（`pip show hscredit` 确认包路径指向本仓库），结果应可信；早期版本的"实测"很可能是针对另一份已安装包（例如非本仓库源码的旧版本）运行的，建议今后的复核都先确认 `pip show hscredit` 的 `Location` 指向当前工作目录，避免基线漂移。

---

## 四、测试套件现状（本次全量实测）

修复前：

```
pytest tests/ -m "not slow and not integration"
462 passed, 3 skipped, 2 failed in 42.15s
```

修复后（含 Diff-3 新增的 1 个测试）：

```
pytest tests/ -m "not slow and not integration"
463 passed, 3 skipped, 2 failed in 53.13s
```

- 2 处失败均为 [`tests/test_models/test_scorecard_export_consistency.py`](../tests/test_models/test_scorecard_export_consistency.py) 的 PMML 导出一致性测试，根因是本机 Java 运行时版本过旧（`UnsupportedClassVersionError`：class file version 55 即 Java 11，本机只支持到 52 即 Java 8），属本机环境问题，**非 hscredit 代码缺陷**，换一台装有 Java 11+ 的机器应可通过。
- `tests/test_report/`、`tests/test_reports/` 等本次 diff 涉及目录全部通过。
- `tests/conftest.py` 当前 `collect_ignore` 共 **8** 个文件（`test_binning/` 下 4 个 + `test_utils/` 下 4 个），均为脚本式/依赖本地环境的测试，未纳入常规 CI 收集范围。

---

## 五、修复优先级建议

| 优先级 | 条目 | 说明 | 状态 |
|--------|------|------|------|
| **P0，立即** | N-1（`import six`） | 影响所有全新环境的 `import hscredit`，应最先修复 | ✅ 已修复 |
| **P1，短期** | N-2（`dataframe_plot`/`score_bin_plot`）、N-3（`vintage_plot`/`bad_rate_trend_plot` 忽略 `ax`）、N-4（CLAUDE.md 安装命令） | 均为"文档承诺的默认用法直接失败/失效"，修复成本低（各 1～数行） | ✅ 已修复（N-4 在本轮修复前已由外部改为 `[boost]`） |
| **P2，短期** | N-5（Cython 误列依赖）、N-6（3 处文档示例）、Diff-2（超链接异常防护一致性） | 影响范围较小，建议随手清理 | ✅ 已修复 |
| **P3，持续** | N-7（Python 3.14 兼容性声明）、Diff-3（测试覆盖缺口）、C-4（`print`→`logging`，仍剩 412 处） | 代码卫生/可观测性，建议纳入既有 `flake8` pre-commit/CI 门禁逐步收敛 | Diff-3 ✅ 已修复；N-7 待 CI 验证（非代码缺陷，本轮未改动）；C-4 本轮按计划跳过 |
| **无需处理** | C-1、C-2、C-3、C-5、C-6 | 重新验证后确认已解决，无需重复修复 | — |

---

## 六、结论

当前未提交的入模变量超链接改动**设计正确、测试齐备**；本次专项复核在 `core/viz` 可视化模块发现的一类可复现真实 bug（`ax` 参数在多个函数中名不副实）、依赖声明问题（`six` 未声明、`Cython` 误列硬依赖）、以及 3 处文档示例与实现不符，**均已在本次会话中修复并通过运行时复现+单测验证**，diff 自身的异常处理一致性疏漏和测试覆盖缺口也已补齐。全量回归 463 passed / 3 skipped / 2 failed（2 处失败为本机 Java 版本过旧导致的 PMML 测试问题，与本次改动无关）。剩余未动的只有 N-7（Python 3.14 兼容性声明，需 CI 矩阵验证而非代码改动）和 C-4（412 处 `print`→`logging`，已确认为持续清理项，本轮按计划跳过）。同时，本次复核证明早期版本报告中标记为"仍待处理"的 5 项（C-1/C-2/C-3/C-5/C-6）在当前仓库源码中已不存在，建议将本文件作为后续复核的新基线，并固定复核环境为本仓库的可编辑安装。
